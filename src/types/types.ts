export type ButtonProps = {
  btnText: string;
  target: string;
};

export type HeadingProps = {
  superHeading: string;
  heading1: string;
  heading2: string;
};

export type ServiceItemProps = {
  id?: number;
  heading: string;
  description: string;
  svg: JSX.Element;
};
